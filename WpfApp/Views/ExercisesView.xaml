<UserControl x:Class="WpfApp.Views.ExercisesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Margin="16">
        <TextBlock DockPanel.Dock="Top" Text="Exercises" FontSize="20" FontWeight="Bold" Margin="0,0,0,12" />

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0" >
            <Button Content="EDIT" Click="EditExercise_Click" Padding="12,6" Margin="4" />
            <Button Content="DELETE" Click="DeleteExercise_Click" Background="#FF6B6B" Padding="12,6" Margin="4" />
            <Button Content="ADD" Click="AddExercise_Click" Padding="12,6" Margin="4" />
        </StackPanel>

        <DataGrid x:Name="ExerciseGrid" 
              AutoGenerateColumns="False"
              IsReadOnly="True"
              CanUserAddRows="False"
              SelectionMode="Single"
              HeadersVisibility="Column" 
              SelectionChanged="ExercisesGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Exercise" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
                <DataGridTemplateColumn Header="Action">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="MY PROGRESS" Click="ViewExercise_Click" Padding="8,2" Tag="{Binding Id}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
